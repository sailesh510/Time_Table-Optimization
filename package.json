{
  "name": "timetable-system",
  "version": "1.0.0",
  "description": "Digital Faculty Timetable Optimization System",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm install && npm run build",
    "server-install": "cd server && npm install",
    "heroku-postbuild": "npm run build && npm run server-install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
